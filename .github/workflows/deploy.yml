# GitHub Actions workflow disabled - using Vercel's native GitHub integration instead
# Vercel automatically deploys on push to main branch when GitHub integration is connected
# To re-enable this workflow, uncomment the code below and ensure GitHub Secrets are configured:
# - VERCEL_TOKEN
# - VERCEL_ORG_ID  
# - VERCEL_PROJECT_ID

# name: Deploy to Vercel
#
# on:
#   push:
#     branches:
#       - main
#       - master
#   pull_request:
#     branches:
#       - main
#       - master
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#
#       - name: Install dependencies
#         run: npm install --legacy-peer-deps
#
#       - name: Build
#         run: npm run build
#         env:
#           NEXT_PUBLIC_COINBASE_DEVELOPER_PLATFORM_API_KEY: ${{ secrets.NEXT_PUBLIC_COINBASE_DEVELOPER_PLATFORM_API_KEY }}
#           GOOGLE_PLACES_KEY: ${{ secrets.GOOGLE_PLACES_KEY }}
#           GPT5_API_KEY: ${{ secrets.GPT5_API_KEY }}
#           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#
#       - name: Deploy to Vercel
#         uses: amondnet/vercel-action@v25
#         with:
#           vercel-token: ${{ secrets.VERCEL_TOKEN }}
#           vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
#           vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
#           vercel-args: '--prod'
